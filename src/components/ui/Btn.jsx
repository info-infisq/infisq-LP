export default function Btn({ label }) {
  return (
    <button className="py-3 px-10 text-white  text-base-custom font-bold bg-gradient-primary rounded-xl cursor-pointer">
      {label}
    </button>
  );
}
